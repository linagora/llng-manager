import {
  transformJsonToList,
  transformListToJson,
} from "../../src/utils/nativeRule";

describe("transformJsonToList", () => {
  it("should expose a function", () => {
    expect(transformJsonToList).toBeDefined();
  });

  it("transformJsonToList should return expected output", () => {
    // const retValue = transformJsonToList(key);
    expect(false).toBeTruthy();
  });
});
describe("transformListToJson", () => {
  it("should expose a function", () => {
    expect(transformListToJson).toBeDefined();
  });

  it("transformListToJson should return expected output", () => {
    // const retValue = transformListToJson(list);
    expect(false).toBeTruthy();
  });
});
